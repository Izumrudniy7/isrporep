def area(a, h):
    """Принимает значения треугольника со стороной а и высотой, проведённой к этой стороне, возвращает её площадь a*h/2"""
    return a * h / 2

def perimeter(a, b, c):
    """Принимает значения треугольника со сторонами а, b, c возвращает его периметр"""
    return a + b + c
